using System.IO;

//internal static class PPMReaderHelpersBaseBase {
//    public static BitmapMaker BitmapFromPPM(string file) {

//        var Reader = new BinaryReader(new FileStream(file, FileMode.Open));

//        if (Reader.ReadChar() != 'P' || Reader.ReadChar() != '3') {
//            return null;
//        }

//    }
//}